# Server
PORT=3011
SESSION_SECRET=dev_secret_change_me

# Same as SSMS (Windows Authentication — no password)
DB_SERVER=AURA\SQLEXPRESS
DB_USE_WINDOWS_AUTH=true
DB_DATABASE=OnlineExamSystem

DB_CONNECTION_TIMEOUT=20000
