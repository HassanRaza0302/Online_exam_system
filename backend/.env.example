# Server
PORT=3011

# Sessions
SESSION_SECRET=dev_secret_change_me

# SQL Server (Microsoft SQL Server 2014)
DB_SERVER=localhost
DB_PORT=1433
DB_DATABASE=OnlineExamDB
DB_USER=sa
DB_PASSWORD=YourStrongPassword123

# If you're using a named instance, set DB_SERVER like:
# DB_SERVER=localhost\\SQLEXPRESS

# For many local SQL Server setups, encryption can cause issues.
# We'll default to encrypt=false and trustServerCertificate=true in code.

